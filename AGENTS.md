# 项目 Agent 工作约定

本文件是所有进入本项目的 AI Agent 的第一入口。无论使用何种模型或工具，开始工作前都必须先阅读本文件和根目录的 `MEMORY.md`。

## 项目目标

为没有编程背景的学习者设计一套 AI 时代全栈产品课程，使其能够在 AI 辅助下完成从数据库、后端、权限到客户端、部署和运维的生产可用产品。

当前项目包含两条相互支持的主线：

1. `AI时代非程序员全栈产品课程大纲.md`：正式课程框架。
2. `Anthropic工程博客学习路径课程.html`：正式产品课的工程判断先修课，把 Anthropic Engineering 文章转化成可执行的判断力训练。

必须区分两条线：完成六阶段先修课不等于完成生产产品；正式产品课仍需逐步完成真实数据库、后端、客户端、权限、部署、监控、备份与恢复。

## 开始工作前的阅读顺序

1. `AGENTS.md`
2. `MEMORY.md`
3. `agent/memory/project_context.md`
4. `agent/memory/progress.md`
5. `agent/memory/decisions.md`
6. 与当前任务相关的角色说明和文章映射

不得只根据聊天记录猜测项目状态。

## 不可破坏的产品原则

- 目标用户是零基础学习者。每个术语都必须能通过生活语言、案例或完成示例理解。
- 文章不能作为孤立外链存在。每篇必读文章必须绑定阶段目标、中文导读、中文精读摘要、阅读问题、练习和产出；课程内阅读默认使用抽屉，关闭后返回当前任务。
- 学习进度不能只由“打开过”或“浏览过”决定，必须至少包含回忆题、项目产出或验收证据。
- 课程的最小学习循环是：读前判断并记录 → 核心概念分块 → 导读与中文精读摘要 → 即时回忆与反馈 → 完成示例 → 引导练习 → 自我解释 → 独立项目迁移 → 24 至 48 小时延迟复习。
- 中文精读摘要不是逐段或完整翻译。涉及数字、方法、引文和边界条件时必须回到英文原文核对。
- 生产可用的定义必须覆盖真实数据、权限、安全、部署、日志、监控、备份和回滚。
- AI 可以生成第一版，但不得作为自己工作的唯一验收者。
- 不复制 OpenAI 或 Anthropic 的 Logo、品牌资产和容易造成官方误认的内容。
- `anthropic-engineering-crawl/` 中的原始全文和抓取摘要属于来源档案，除修复事实错误外不要重写。

## 文件职责

| 文件或目录 | 职责 |
|---|---|
| `AI时代非程序员全栈产品课程大纲.md` | 总课程设计和课题结构 |
| `Anthropic工程博客学习路径课程.html` | 学员直接使用的学习工作台 |
| `教育审查与优化报告.md` | 逐章、逐文章、逐文件的教育审查结论和未解决风险 |
| `anthropic-engineering-crawl/anthropic_engineering_articles_classification.html` | 25 篇文章的可搜索分类报告 |
| `anthropic-engineering-crawl/anthropic_engineering_articles_zh.html` | 25 篇文章的非官方中文精读摘要阅读器 |
| `anthropic-engineering-crawl/*.md`、`*.json` | 原始抓取、全文和分类依据 |
| `agent/memory/` | 跨会话长期记忆 |
| `agent/roles/` | 不同 Agent 的职责和交接条件 |
| `agent/templates/` | 课程单元、任务和决策记录模板 |
| `scripts/validate_project.py` | 项目结构和关键数量验证 |

## 标准工作流程

1. **Explore**：阅读当前页面、来源材料、进度和相关决策。
2. **Plan**：说明用户核心路径、改动范围、成功条件和不改内容。
3. **Implement**：按现有视觉和内容系统做最小完整改动。
4. **Verify**：运行项目校验，并检查移动端、键盘操作、链接和学习闭环。
5. **Record**：更新 `MEMORY.md`、`agent/memory/progress.md`；有新取舍时更新 `decisions.md`。

## 内容改动规则

- 每个阶段必须回答：读前先判断什么、要建立哪 3 至 6 个核心概念、为什么学、读哪篇中文精读摘要、带着什么问题读、看什么示例、如何补全引导练习、如何用自己的话解释、独立做什么、如何证明完成、隔天复习什么。
- 必读文章要少而清晰；延伸文章放在选读，不阻塞主线。
- 中文导读只能归纳原文，不得把推断伪装成作者结论。
- 原文标题、链接和分类必须与 `article_stage_map.md` 对齐。
- 新增文章时必须同时更新分类报告、阶段映射、课程页和验证规则。

## 前端改动规则

- 保持单文件静态 HTML，可直接通过本地文件打开。
- 不引入远程 JavaScript、字体、图标或分析脚本。
- 学习状态只保存在浏览器本地，不上传。
- 所有主要操作必须有清晰文本、键盘焦点和移动端布局。
- 避免大面积营销式 Hero；第一屏要能开始或继续学习。
- 卡片圆角不超过 8px，避免无意义阴影和装饰。

## 完成标准

完成任务前至少运行：

```bash
python3 scripts/validate_project.py
```

同时确认：

- 本地链接存在。
- HTML 没有重复 ID。
- 6 个阶段、12 篇必读文章、6 个即时回忆、6 个引导练习、18 个持久化回答、6 个延迟复习和 25 篇分类文章完整。
- “继续学习”能够定位到第一项未完成任务。
- 新增功能拥有空状态、错误提示或恢复方法。
- 相关记忆文件已经更新。

## Agent 角色

- 课程研究：`agent/roles/curriculum_researcher.md`
- 学习体验设计：`agent/roles/learning_designer.md`
- 内容融合：`agent/roles/content_integrator.md`
- 质量审查：`agent/roles/qa_reviewer.md`

一个 Agent 可以承担多个角色，但必须按角色的交付条件逐项自检。
