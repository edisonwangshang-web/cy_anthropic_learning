# AI 时代·非程序员全栈产品搭建课程大纲

> **课程目标**：让零编程基础的学员，在 AI 辅助下，独立完成一个**从数据库到客户端、可真实上线、生产可用**的完整产品（而非 demo）。
>
> **学员角色定位**：产品负责人 + 技术指挥 + 验收者。不是码农，而是"会判断、会验证、会兜底"的人。
>
> **核心矛盾**：难点不在"让 AI 写代码"，而在"非程序员如何判断、验证、兜底"。整门课的重心是 **demo → 生产** 的跨越。

> **与配套先修课的关系**：`Anthropic工程博客学习路径课程.html` 只训练工程判断、规格、安全、评测和故障复盘，是进入本课程的先修模块，不替代下面的数据库、后端、客户端、权限、部署和运维实作。学员只有完成真实上线与恢复证据，才算达到本大纲的课程目标。

---

## 一、课程整体设计

### 1.1 设计原则

1. **不教语法，教判断**：学员不需要会手写代码，但必须能看懂 AI 在干什么、判断对错、识别风险。
2. **一个真实项目贯穿全程**：建议选「带用户系统的预约/记账/工单平台」之类——每章都在同一产品上加东西，最后真上线、真有用户。
3. **重心后置**：和市面上"AI 做 demo"教程的根本区别在模块 6/7/8（安全/上线/稳定），课时配比向这三块倾斜。
4. **永远不信任 AI 的第一版**：贯穿全程的黄金心法。

### 1.2 "生产可用" vs "demo" 的验收标准（贯穿全课）

| 维度 | demo（不合格） | 生产可用（目标） |
|------|---------------|----------------|
| 数据 | 假数据/内存数据，刷新就没 | 真实数据库 + 定期备份 |
| 稳定性 | 本机能跑 | 部署上线、24h 可访问、崩了能恢复 |
| 安全 | 接口裸奔、密码明文 | 登录鉴权、数据加密、防注入 |
| 错误处理 | 一出错就白屏 | 友好提示 + 后台可查日志 |
| 可维护 | 改一处崩三处 | 能持续迭代、能回滚 |
| 用户 | 只有自己点过 | 有真实用户在用 |

### 1.3 入学门槛说明（划清"非程序员"下限）

- **最低要求**：会装软件、会基本文件操作、不怕命令行（能跟着敲）。
- **不适合**：完全不碰电脑配置、看到英文报错就放弃的人——需先补"数字素养"。
- 建议入学前发一份「环境自检清单」筛一遍。

### 1.4 工具策略（取舍点）

- **主线**：讲通用判断力（寿命长、不过时）。
- **实操**：绑定一套 AI 编程工具（AI IDE / AI+低代码）做贯穿项目，落地快。
- 课程中标注"工具会变，但判断逻辑不变"，降低学员对单一工具的依赖。

---

## 二、模块详解

> 每个模块包含：**学习目标 → 核心内容 → 本章产出（贯穿项目里加了什么）→ 验收点**

---

### 模块 0：思维转换（约 5%）

**学习目标**：完成从"我不会编程所以做不了"到"我是指挥者"的心态切换。

**核心内容**
- AI 时代你的真实角色：指挥 + 验收，不是执行。
- "生产可用"到底意味着什么（建立 1.2 那张验收表的认知）。
- 黄金心法：永远不信任 AI 的第一版——它会自信地犯错（幻觉）。
- 心理建设：会卡住、会报错是常态，关键是"如何和 AI 一起脱困"。

**本章产出**：写下自己要做的产品一句话定义 + 它的"生产可用"验收标准。

**验收点**：能说清自己产品"做成什么样才算上线"。

---

### 模块 1：看懂一个产品由什么构成（地基）

**学习目标**：能听懂 AI 说的话、看懂报错大概在说什么，不再"全是黑话"。

**核心内容**
- 产品解剖：客户端 ↔ 后端 ↔ 数据库，数据怎么流（一张图讲透）。
- 概念扫盲（只为听懂，不为手写）：前端 / 后端 / API / 数据库 / 部署 / 域名 / 环境 / 服务器。
- 技术选型：教学员**让 AI 帮你选技术栈**，并判断它的建议靠不靠谱（问它"为什么选这个、有什么坑"）。

**本章产出**：和 AI 一起确定贯穿项目的技术栈，并写下"为什么选它"。

**验收点**：能用大白话画出自己产品的"数据流向图"。

---

### 模块 2：和 AI 高效对话（核心技能）

**学习目标**：掌握"把模糊想法 → AI 能执行的清晰需求"的能力（本质是写规格）。

**核心内容**
- 需求拆解：把"我想要个预约系统"拆成 AI 能一步步实现的小任务。
- 提需求三要素：给上下文、给约束、给验收标准。
- AI 卡住/跑偏时的脱困技巧：拆小、贴报错、要求解释、换思路、让它给多个方案。
- 读懂 AI 给的代码和报错——不用会写，要看懂在干嘛、改对没。

**本章产出**：一份自己项目的"需求拆解清单"（按模块拆好的待办）。

**验收点**：能把一个模糊功能，独立拆成 3~5 条 AI 可执行的清晰指令。

---

### 模块 3：数据库——产品的根基（重点 · 第一道分水岭）

**学习目标**：理解数据怎么存、怎么不丢，能审 AI 的数据设计合不合理。

**核心内容**
- 数据设计基础：表、字段、关系（让 AI 设计，你审"这么存合理吗、漏了啥"）。
- 真实数据 vs 玩具数据：数据量、增长、迁移。
- **demo→生产 第一道分水岭**：玩具项目用内存假数据，生产必须真数据库 + 备份。
- 备份与恢复初识（模块 8 深入）。

**本章产出**：贯穿项目的数据库设计 + 真实建库建表。

**验收点**：数据存进真实数据库、重启不丢、能说清每张表为什么这么设计。

---

### 模块 4：后端与 API——业务逻辑

**学习目标**：能指挥 AI 实现业务规则，并守住"校验"和"错误处理"两道关。

**核心内容**
- 业务规则交给 AI 实现（例如"同一时段不能重复预约"）。
- 数据校验：用户乱填、恶意输入怎么办（前后端都要校验）。
- 错误处理：出错别白屏，要有兜底和提示。
- 接口设计的基本判断：这个接口该返回什么、该不该公开。

**本章产出**：项目的核心业务接口跑通 + 带校验和错误处理。

**验收点**：故意输入垃圾数据，系统不崩、给出合理提示。

---

### 模块 5：客户端——用户看到的部分

**学习目标**：能让 AI 生成界面，并以"用户体验验收者"的身份把关。

**核心内容**
- 页面与交互让 AI 生成，你负责验收体验好不好用。
- 必备件：表单、列表、加载状态、成功/失败反馈。
- 响应式 / 移动端基本要求（手机上不能错位）。
- 体验细节：空状态、加载中、操作确认。

**本章产出**：项目主要页面可用、手机电脑都能正常看。

**验收点**：让一个外行用你的界面完成一次完整操作，不用你解释。

---

### 模块 6：用户系统与安全（最大盲区 · 独立成章）

**学习目标**：守住产品"敢不敢真给陌生人用"的安全底线。

**核心内容**
- 注册 / 登录 / 权限：自己做 vs 用现成服务（**强烈建议用现成的鉴权服务**）。
- 安全红线（必须逐条讲，这是非程序员最致命的盲区）：
  - 密码不能明文存储
  - 敏感数据别泄露给前端
  - 接口别裸奔（要鉴权）
  - SQL 注入 / 越权访问是什么、怎么防
- 隐私合规意识：收集了哪些用户数据、能不能这么用。

**本章产出**：项目接入完整登录鉴权 + 过一遍安全检查清单。

**验收点**：未登录用户无法访问受保护数据、密码在数据库里是加密的。

---

### 模块 7：从"能跑"到"能上线"（demo → 生产 · 总分水岭）

**学习目标**：把本机能跑的东西，变成全世界能访问的真实产品。

**核心内容**
- 部署：把产品放到真实服务器/平台。
- 域名 + HTTPS 配置（https 是基本门面，也是安全要求）。
- 环境区分：开发环境 vs 生产环境——**别拿用户真实数据做实验**。
- 上线前检查清单（安全/备份/错误处理/性能都过一遍）。

**本章产出**：项目正式上线，有公网地址，别人能访问。

**验收点**：用别人的手机、别人的网络，能打开并正常使用你的产品。

---

### 模块 8：稳定与排错（生产的命脉）

**学习目标**：产品出问题时，能定位、能修、能恢复，不抓瞎。

**核心内容**
- 排错方法论：看日志 → 假设 → 验证 → 定位 → 让 AI 帮 debug。
- 监控与告警：东西挂了第一时间知道。
- 数据备份与恢复演练：最坏情况（数据删了/服务器挂了）怎么救。
- 性能基本意识：哪些操作会变慢、用户多了会不会炸。

**本章产出**：项目配好日志 + 定期备份 + 一次"故障恢复演练"。

**验收点**：模拟一次故障（如删错数据），能靠备份恢复回来。

---

### 模块 9：迭代与维护（产品是活的）

**学习目标**：上线后能持续改进、加功能而不搞崩老功能。

**核心内容**
- 安全地改：加功能时怎么不破坏已有功能。
- 版本管理基本概念：让 AI 帮你管，但你要懂"出事了能回滚"。
- 成本意识：服务器、AI/API 调用、数据库都要花钱，怎么控。
- 收集真实用户反馈 → 排优先级 → 迭代。

**本章产出**：在已上线产品上，安全地新增一个功能并发布。

**验收点**：新增功能后，老功能仍正常；能演示一次回滚。

---

### 模块 10：毕业项目

**学习目标**：独立完成一个生产可用、有真实用户的产品。

**核心内容**
- 从 0 到上线独立走完全流程（可与贯穿项目合并，或另起新题）。
- 对照模块 0 立下的"生产可用"验收表逐项自检。
- 成果展示 + 互评 + 真实用户反馈。

**毕业标准**
- ✅ 真上线、有公网地址
- ✅ 有真实数据库 + 备份
- ✅ 有登录鉴权 + 过安全检查
- ✅ 出错不白屏、有日志可查
- ✅ 有至少 N 个真实用户使用过

---

## 三、课时配比建议

| 模块 | 主题 | 建议占比 | 说明 |
|------|------|---------|------|
| 0 | 思维转换 | 5% | 心态地基 |
| 1 | 产品构成 | 8% | 概念扫盲 |
| 2 | 和 AI 对话 | 12% | 核心技能 |
| 3 | 数据库 | 12% | 第一道分水岭 |
| 4 | 后端/API | 12% | 业务逻辑 |
| 5 | 客户端 | 10% | 界面验收 |
| 6 | 用户系统与安全 | **13%** | 最大盲区（加重） |
| 7 | 上线部署 | **12%** | 总分水岭（加重） |
| 8 | 稳定与排错 | **10%** | 生产命脉（加重） |
| 9 | 迭代维护 | 4% | 长期视角 |
| 10 | 毕业项目 | 2%（+课外） | 综合验收 |

> **配比核心思想**：模块 2/3/6/7/8 合计约 59%，把资源压在"和 AI 协作"与"demo→生产的跨越"上——这正是非程序员最容易死、也最区别于普通教程的地方。

---

## 四、贯穿项目设计建议

- **选题原则**：麻雀虽小五脏俱全——必须同时涉及"数据库 + 业务逻辑 + 用户系统 + 界面"。
- **推荐候选**：
  1. 小型预约平台（理发/会议室/课程预约）
  2. 团队工单/任务系统
  3. 个人记账/小型财务工具
- **真实性要求**：最后必须有真实用户（哪怕是身边 5 个人）真的用起来，才算闭环。

---

## 五、与市面教程的差异点（卖点）

1. **不止于 demo**：别人讲到"功能跑通"就结束，本课的重头戏才刚开始（安全/上线/稳定/维护）。
2. **教判断而非语法**：培养"技术判断者"，而非"复制粘贴工"。
3. **真实闭环**：毕业标准是"真上线 + 真用户"，不是交个作业。
4. **风险意识**：把非程序员最致命的安全/数据盲区单独成章、反复强调。
